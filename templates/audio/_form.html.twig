{{ form_start(form) }}     
    <div class="mb-3">
        {{ form_row(form.genre)}}
    </div>  
    <div class="mb-3">
        {{ form_row(form.file)}}
    </div>  
    <div class="mb-3">
        {{ form_row(form.description)}}
    </div>
    <div class="mb-3">
        <button class="btn app-btn-primary">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}