{{ form_start(form) }}    
    <div class="mb-3">
        {{ form_row(form.title)}}
        {{ form_errors(form.title)}}
    </div>
    <div class="mb-3">
        <button class="btn app-btn-primary">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}