<form method="post" action="{{ path('toplines_delete', {'id': topline.id}) }}" onsubmit="return confirm('Confirmer la suppression?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topline.id) }}">
    <button class="btn btn-danger mb-3">Supprimer</button>
</form>
