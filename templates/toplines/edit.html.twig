{% extends 'base.html.twig' %}

{% block title %}Keepvibz | Modifier {{topline.title}}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('user-profile') }}
{% endblock %}


{% block body %}
    <div class="main">
    <a class="link" href="{{path('user_profile')}}">Retour</a>    
	    <div class="form-wrap">
            <h1 class="main-title">Modifier {{topline.title}}</h1>
                {{ include('toplines/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
                {{ include('toplines/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
