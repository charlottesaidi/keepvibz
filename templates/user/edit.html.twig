{% extends 'admin-base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="app-wrapper">
    
	    <div class="app-content pt-3 p-md-3 p-lg-4">
		    <div class="container-xl">
                <div class="row g-3 mb-4 align-items-center justify-content-center">
			        <h1 class="app-page-title mb-0 text-center">Modifier cet utilisateur</h1>
                    <div class="col-sm-6">
                        <div class="app-card app-card-settings shadow-sm p-4">
                            <div class="app-card">
                            {{ include('user/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
                            {{ include('user/_delete_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
